// renders the modal that shows which pokemon can
// learn a clicked move, whether or not that move is an HM or TM,
// and what specifically the move does in the context of contests
function MoveModal() {
  return (
    <div>
      <p> This is the MoveModal starter</p>
    </div>
  );
}

export default MoveModal;
